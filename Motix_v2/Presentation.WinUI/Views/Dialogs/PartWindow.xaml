<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Motix_v2.Presentation.WinUI.Views.Dialogs.PartWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ct="using:CommunityToolkit.WinUI.UI.Controls"
    Title="Seleccionar Pieza">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Búsqueda -->
            <RowDefinition Height="*"/>
            <!-- Tabla de resultados -->
            <RowDefinition Height="Auto"/>
            <!-- Cantidad y precios -->
            <RowDefinition Height="Auto"/>
            <!-- Botones -->
        </Grid.RowDefinitions>

        <!-- Fila 0: Búsqueda -->
        <StackPanel
            Grid.Row="0"
            Orientation="Horizontal"
            Padding="16"
            Spacing="8">
            <TextBlock Text="Ref Interna:" VerticalAlignment="Center"/>
            <TextBox x:Name="TextBoxSearchReferenciainterna" Width="150"/>
            <TextBlock Text="Nombre:" VerticalAlignment="Center" Margin="16,0,0,0"/>
            <TextBox x:Name="TextBoxSearchName" Width="150"/>
            <Button
                x:Name="ButtonSearchPart"
                Content="Buscar"
                Margin="16,0,0,0"/>
        </StackPanel>

        <!-- Fila 1: Tabla de piezas -->
        <ct:DataGrid
            x:Name="DataGridParts"
            Grid.Row="1"
            AutoGenerateColumns="True"
            ColumnWidth="Auto"
            IsReadOnly="True"
            SelectionMode="Single"
            Margin="16,0"/>

        <!-- Fila 2: Cantidad y precios -->
        <Grid
            Grid.Row="2"
            Padding="16"
            ColumnDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">
            <TextBlock Text="Cantidad:" VerticalAlignment="Center"/>
            <TextBox x:Name="TextBoxQuantity" Grid.Column="1" Width="80" Margin="8,0"/>
            <TextBlock Text="Precio U.:" Grid.Column="2" VerticalAlignment="Center" Margin="16,0"/>
            <TextBlock x:Name="TextBlockUnitPrice" Grid.Column="3" Width="80" VerticalAlignment="Center" Margin="8,0"/>
            <TextBlock Text="Total:" Grid.Column="4" VerticalAlignment="Center" Margin="16,0"/>
            <TextBlock x:Name="TextBlockTotalPrice" Grid.Column="5" Width="80" VerticalAlignment="Center" Margin="8,0"/>
        </Grid>

        <!-- Fila 3: Botones -->
        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Padding="16"
            Spacing="8">
            <Button x:Name="ButtonCancel" Content="Cancelar" Width="100"/>
            <Button x:Name="ButtonConfirm" Content="Confirmar" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
